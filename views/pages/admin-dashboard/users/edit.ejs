<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <link rel="stylesheet" href="/css/users/edit.css">
</head>
<body>
<div class="edit-user-form">
    <div class="euf-container">
        <div class="euf-header">
            <h1>User Management</h1>
        </div>

        <nav>
            <ol class="euf-breadcrumb">
                <li class="euf-breadcrumb-item"><a href="/admin/users">Users</a></li>
                <li class="euf-breadcrumb-item">Edit User</li>
            </ol>
        </nav>

        <div class="euf-user-id">
            <strong>User ID:</strong> <span id="eufUserId"><%= user.user_id %></span>
        </div>

        <form action="/edit/<%= user.user_id %>?_method=PUT" method="POST" id="eufEditUserForm">
            <div class="euf-form-row">
                <div class="euf-form-col">
                    <div class="euf-form-group">
                        <label class="euf-label" for="eufFirstname">First Name</label>
                        <input type="text" id="eufFirstname" name="firstname" class="euf-input" value="<%= user.firstname %>" required>
                    </div>
                </div>
                <div class="euf-form-col">
                    <div class="euf-form-group">
                        <label class="euf-label" for="eufLastname">Last Name</label>
                        <input type="text" id="eufLastname" name="lastname" class="euf-input" value="<%= user.lastname %>" required>
                    </div>
                </div>
            </div>

            <div class="euf-form-group">
                <label class="euf-label" for="eufEmail">Email</label>
                <input type="email" id="eufEmail" name="email" class="euf-input" value="<%= user.email %>" required>
            </div>

            <div class="euf-form-row">
                <div class="euf-form-col">
                    <div class="euf-form-group">
                        <label class="euf-label" for="eufPhoneNumber">Phone Number</label>
                        <input type="text" id="eufPhoneNumber" name="phone_number" class="euf-input" value="<%= user.phone_number %>" required>
                    </div>
                </div>
                <div class="euf-form-col">
                    <div class="euf-form-group">
                        <label class="euf-label" for="eufPhoneType">Phone Type</label>
                        <select id="eufPhoneType" name="phone_type" class="euf-select" required>
                            <option value="Mobile" <%= user.phone_type === 'Mobile' ? 'selected' : '' %>>Mobile</option>
                            <option value="Home" <%= user.phone_type === 'Home' ? 'selected' : '' %>>Home</option>
                            <option value="Work" <%= user.phone_type === 'Work' ? 'selected' : '' %>>Work</option>
                        </select>
                    </div>
                </div>
            </div>

            <div class="euf-form-group">
                <label class="euf-label" for="eufRole">Role</label>
                <select id="eufRole" name="role" class="euf-select" required>
                    <option value="1" <%= user.role === '1' ? 'selected' : '' %>>Admin</option>
                    <option value="2" <%= user.role === '2' ? 'selected' : '' %>>User</option>
                    <option value="3" <%= user.role === '3' ? 'selected' : '' %>>Guest</option>
                </select>
            </div>

            <div class="euf-form-group">
                <button type="submit" class="euf-submit-btn">
                    <i class="fas fa-save"></i> Update User
                </button>
            </div>
        </form>
    </div>
</div>
</body>
</html>